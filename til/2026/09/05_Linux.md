# cd コマンド（抜粋）

- `cd`：ディレクトリを移動する基本コマンド
- `cd ディレクトリ名`：指定したディレクトリへ移動
- `cd ..`：1つ上の階層へ戻る
- `cd ../..`：2つ上の階層へ戻る
- `cd ~`：ホームディレクトリへ移動
- `cd /path/to/dir`：絶対パスで移動
- `cd ./dir`：現在位置から相対パスで移動
- `cd -`：直前のディレクトリへ戻る（履歴切替）
- `pwd`：現在のディレクトリを確認（cdとセットで使う）
