# 基本コマンド

pwd            # 現在のディレクトリを表示 
pwd -P         # シンボリックリンクを解決して実パスを表示 
cd ..          # 1つ上の階層へ移動（pwdで確認） 
cd /path       # 絶対パスへ移動（pwdで確認） 
ls             # 中身を確認して pwd の位置を把握 
echo $PWD      # 環境変数から現在位置を取得 
