# ls コマンド

- `ls`：現在のディレクトリのファイル一覧を表示
- `ls -l`：詳細表示（権限・所有者・サイズ・更新日時）
- `ls -a`：隠しファイル（.xxx）も含めて表示
- `ls -la`：詳細＋隠しファイル（最もよく使う）
- `ls -lh`：サイズを KB/MB で読みやすく表示
- `ls -lt`：更新日時の新しい順に並べる
- `ls -ltr`：更新日時の古い順（ログ確認で便利）
- `ls *.py`：特定の拡張子だけ表示（例：Pythonファイル）
- `ls -1`：1行1ファイルでシンプルに表示
- よく使う組み合わせ：`ls -lah`
