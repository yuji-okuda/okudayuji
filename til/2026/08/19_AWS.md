# ENI(Elastic Network interface）
・仮想ネットワークインターフェース
・EC2インスタンスのIPアドレスはENIに付与される

# Elastic IP address（EIP)
・インスタンスにアタッチとデタッチ可能で静的なパブリックIPアドレス ・インスタンスが再起動、停止、終了しても同一アドレス


# Security group（AWSの仮想ファイアウォールサービス）　ENI単位で行う
・拒否のルールは設定できず、許可の設定のみ可能

# ネットワークACL　サブネット単位で行う
